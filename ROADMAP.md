# HubPooL 产品路线图

面向未来 6 个月，目标场景是小团队协作（几个人各自跑本地 HubPooL 实例，通过 P2P 同步对齐数据）。采用 Now / Next / Later 的形式：Now 是这两个月要交付的地基，Next 是建立在地基上的协作与生态打通，Later 是方向明确但时间和范围仍有弹性的智能化能力。

## 当前基线：v0.2.0

已经落地、可以直接作为后续规划起点的能力：多项目需求管理，列表/看板/版本视图，批量操作，评审模式，周报，CSV/JSON 导入导出；活态批注讨论，AI 需求评审（复制往返，不存密钥），需求级版本历史；服务实例间的真实 P2P 云同步（推送/拉取/合并、同步前变更预览、自定义同步说明、同步历史）；现代极简 UI（线条图标、统一设计系统、可折叠侧边栏）；系统设置分类整合。详见 [CHANGELOG.md](./CHANGELOG.md)。

这条基线里已经有的东西，下面不再重复规划——比如同步机制本身是稳的，接下来的"协作与权限"是在这个同步机制之上补身份和角色，不是重做同步。

## Now（0–2 个月）：协作地基

分发方式保持现状：Node/npm + `启动需求池.command` 一键脚本（首次自动装依赖、build，之后直接起服务），不引入独立安装包或桌面打包，维持轻量化。这两项是接下来所有方向的前提——没有身份就没法做角色和归属。

**本地成员身份（轻量级，不做账号系统）**
给每个人在本机设置一个"我是谁"的身份标签，用于批注署名、同步记录归属、需求变更历史里的操作人。不做密码、不做登录，纯本地设置项，和现有的 P2P 信任模型（同一局域网、对方主动同步）保持一致。

**角色标签与关键操作确认**
产品/设计/研发/测试/业务五类角色标签（复用 ROADMAP 里原有的分类），关键操作（删除项目、改设置、执行同步）加二次确认和操作记录。P2P 场景下做服务端强制权限校验意义有限，先做"标注 + 软限制 + 可追溯"，不做企业级 RBAC。

## Next（2–4 个月）：协作与生态打通

**发布计划与版本回滚**
现有的"版本"对象升级为发布计划：里程碑、风险清单、评审签核；需求版本支持选择字段回滚，Diff 展示更清晰。这条是 ROADMAP 里原计划的延续。

**Jira / Linear 最小闭环集成**
需求创建 + 状态双向映射，不追求评论双向同步、不追求附件同步。目标是打通"从 HubPooL 提需求，同步到 Jira 走排期"，而不是替代 Jira。

**同步与变更通知**
基于已有的同步历史机制，扩展出企业微信/钉钉/Slack 的 Webhook 通知——谁推送了、有没有冲突、需要谁去看。

**自动更新**
延续现有轻量模式：复用系统设置里已有的软件更新检测雏形（对接 GitHub Releases，更新前自动备份、不覆盖 SQLite 数据库），做成"检测新版本 → 提示 → 一键拉取更新文件"，不依赖安装包或打包工具。

## Later（4–6 个月）：智能化能力

这部分方向明确，但具体范围和时间点会随 Now/Next 的实际进度调整，不做硬承诺。

- 相似需求推荐：先用关键词/文本相似度起步，不引入模型 API 依赖
- RICE/MoSCoW 辅助排序：接入已有的优先级、工作量字段给出排序建议，人工最终决定
- 需求依赖关系与关键路径可视化
- GitHub Issue/PR 关联

## 明确不在 6 个月内承诺的能力

互联网公开部署、实时多人同时编辑（当前是异步 P2P 同步，不是协同编辑）、云端账号体系、移动端访问（网页响应式或原生 App 均不在本轮范围）、独立安装包/桌面打包（保持 Node/npm + 启动脚本的轻量分发）、自动执行 Git 命令、模型 API 直连和计费管理、企业级 RBAC。这些都需要独立的安全设计和更大的投入，贸然做会拖慢上面几条的交付。

## 依赖与风险

成员身份是角色权限的前置依赖，顺序不能颠倒——先有"我是谁"，角色标签和操作归属才有意义。

Jira/Linear 集成依赖对方 API 的稳定性和权限申请，这是外部依赖，不能完全控制时间线。

轻量分发意味着团队成员仍需要自己具备装 Node、跑启动脚本的能力，这是主动接受的门槛，不在本轮解决——如果后续发现这个门槛实际挡住了协作推进，需要重新评估打包分发的优先级。

## 容量提示

去掉桌面打包后，Now 阶段的范围更聚焦（只剩身份 + 角色），风险主要集中在 Next 阶段的 Jira/Linear 集成和自动更新细节（备份/回滚要做扎实，避免更新搞坏本地数据）。建议顺序不变：协作地基（身份 + 角色）先在 Now 做实，Next 里如果 Jira 集成调研成本明显超预期，优先保住同步通知和自动更新，因为这两项不依赖外部系统、风险更可控。
