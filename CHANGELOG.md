# 更新日志

## v0.9.6

### 需求池查找与浏览
- 核心统计收拢为总需求、待评审、进行中、逾期和 P0 未完成，点击即可形成对应筛选
- 搜索范围扩展到需求 ID、名称、模块、业务描述、原始描述和功能点
- 常用筛选与更多筛选分层，新增流程阶段筛选；生效条件支持逐项移除和一键清空
- 列表以当前流程阶段为主，收拢冗余字段与行内按钮，统一展示逾期、滞留和未分配发布风险
- 新增舒适/紧凑密度以及项目、类型、交付时间、最近更新列偏好，并保存到本机浏览器

### 需求处理闭环
- 单条需求保存会等待 SQLite 返回结果，失败时恢复操作前数据；关闭未保存表单时明确确认
- 批量状态、优先级和发布计划操作统一反馈成功与跳过数量，保存失败自动恢复前端数据
- 拒绝和挂起需求必须填写原因，并写入需求流转记录
- 局域网游客不再显示列表选择框，只保留查看和复制等只读能力

### 批量录入
- 批量录入预览增加可创建、警告和阻断三级结果，逐条展示字段问题
- 正确需求可以先创建，阻断项保留在预览中继续修改，不再互相影响
- 创建完成后可直接筛选查看本次刚录入的需求

## v0.9.5

### 合并升级范围
- 原计划 v0.9.6 与 v0.9.7 的产品收拢内容统一并入 v0.9.5，不再拆分后续版本号
- 需求池继续作为唯一需求数据源，发布计划、资料中心和流程看板复用同一套范围、风险、阶段与资料信号
- 保持本地优先、可信局域网只读协作边界，不引入云账号、企业 RBAC、实时协作或前端框架重写

### 设置与访问边界
- 系统设置重组为身份与主题、访问与权限、团队同步、外部连接、数据安全、AI 工作流和系统信息
- 本机服务状态、当前访问者、监听范围和局域网游客权限从高级折叠项提升为独立页面
- 全项目任务平台配置与 Git 数据交付统一收拢到外部连接，软件更新与还原独立归入系统信息
- 修正角色说明：工作台仅本机产品角色可见，研发、测试、设计、业务和游客默认进入需求池
- 服务端继续作为最终权限门禁，局域网游客不能调用状态写入、同步、任务同步、AI、快照恢复和软件维护接口

### 需求录入可靠性
- 标准 Markdown 批量录入优先识别 `REQ-*` 需求章节，并支持基本信息列表、字段别名和多级正文标题
- 批量创建修复日期校验器作用域错误，避免只创建快照但不保存需求
- 确认操作改为等待 SQLite 保存结果，保存失败时保留弹窗并回滚未落库数据
- 文档项目无法精确匹配时显示警告，不再静默使用默认项目

### 需求池交互修复
- 批量操作菜单打开时临时提升操作栏层级，修复吸顶表头遮挡菜单
- 菜单关闭后自动恢复默认层级，保持表头正常吸顶
- 页面在主体解析前应用最近确认的主题，首次无缓存时等待服务端主题确定后再显示，消除进入系统时的主题切换闪烁
- 版本号升级到 v0.9.5，同步 CLI、健康检查、前端版本信息、README 和测试断言

## v0.9.4

### 三模块联动收口
- 版本号升级到 v0.9.4，并同步 CLI、健康检查、前端版本信息、README 和测试断言
- 健康检查接口更新 `upgradeRange: 0.9.1-0.9.4`，标记本轮交付闭环升级范围
- 发布计划卡片新增“看流程 / 看资料”入口，流程看板和原型库可围绕同一发布计划轻量联动
- 三个模块继续使用同一套需求、发布计划、资料和风险信号，不新增独立数据模型

## v0.9.3

### 流程看板升级
- 流程看板从发布计划分列调整为按需求阶段分列，回到“需求卡在哪里”的核心问题
- 看板顶部新增流程健康摘要，展示待评审、开发中、测试中、待上线、滞留、逾期、未分配发布和 P0 缺资料
- 需求卡片补充所属发布计划、资料信号、阶段停留天数、逾期和项目范围信息
- 拖拽进入已上线前增加发布说明提醒；已上线回退需要填写原因并写入阶段历史

## v0.9.2

### 原型库资料中心升级
- 原型库保留资料中心定位，主对象调整为资料卡片，而不是发布计划附属列表
- 新增资料类型、资料状态、关联发布计划、搜索和排序筛选
- 新增资料缺口提醒，提示缺原型的发布计划、未关联资料的 P0 需求和超过 30 天未更新的资料
- 资料卡片保留弱关联入口，可查看详情、打开资料、下载或跳回关联发布计划

## v0.9.1

### 发布计划就绪升级
- 发布计划列表新增项目级发布就绪摘要，展示发布计划总数、就绪数、风险数和资料/签核待补齐数
- 发布计划卡片新增就绪检查标签，覆盖目标、负责人、范围、待评审、逾期、签核和发布说明
- 发布计划排序优先暴露高风险、资料缺失和需关注版本，让用户先处理最影响发布的事项

## v0.9.0

### 发布候选升级
- 版本号升级到 v0.9.0，并同步 CLI、健康检查、前端版本信息、README 和测试断言
- 健康检查接口新增 `releaseStage: release-candidate`，用于区分正式试运行版本状态
- 健康检查接口新增 `upgradeRange: 0.5.0-0.9.0`，标记本次补齐的阶段升级范围
- 工作台新增“发布前检查”面板，集中呈现需求评审、逾期风险、发布计划风险和访问边界
- 当前访问者数据加载后会同步刷新发布前检查，便于本机维护者确认局域网访问状态

### 发布质量收口
- 保留 v0.4.0 已完成的信息架构、角色化工作台、局域网游客只读和发布包清单约束
- 本次升级不引入桌面打包、云账号、实时协同或前端框架重写，重点收拢现有功能的发布完整度

## v0.8.0

### 局域网试运行收口
- 本机工作台的发布前检查补充当前在线访问者和局域网访问数量
- 局域网访问继续固定识别为游客，只保留只读浏览能力
- 访问者数据刷新后同步更新发布前检查，降低“别人正在看但本机不知道”的试运行风险

## v0.7.0

### 数据安全收口
- 发布前检查补充数据安全项，根据快照操作记录提示最近快照状态
- 对已有需求但没有快照记录的场景给出“需备份”提示
- 保持现有快照、导入覆盖、恢复和操作日志能力，不引入新的备份存储方案

## v0.6.0

### 发布治理收口
- 发布前检查补充发布资料完整性，检查发布目标、负责人、签核和发布说明
- 发布计划风险继续复用现有版本健康度，不新增独立状态体系
- 让版本详情里已有的发布计划字段进入首页质量门禁，而不是只停留在详情页

## v0.5.0

### 角色入口收口
- 工作台调整为产品角色专属入口，研发、测试、设计、业务和游客不再显示工作台菜单
- 非产品角色默认进入需求池，继续使用需求池、发布计划、原型库和流程看板完成协作
- 保留本机身份设置，产品角色可切回工作台，局域网访问仍固定为游客只读

## v0.4.0

### 信息架构升级
- 版本号升级到 v0.4.0，并同步 CLI、健康检查、前端版本信息和 README
- 主导航从通用标签收拢为“角色工作台 / 需求池 / 发布计划 / 原型库 / 流程看板”，模块职责更清晰
- 顶部新增页面上下文条，展示当前模块说明、项目范围和本机角色
- 需求筛选控件只在“需求池”页面显示，发布、原型、看板和工作台不再被需求筛选栏干扰

### 工作台产品化
- 工作台新增“产品结构”模块地图，集中说明并跳转到需求池、发布计划、原型库、流程看板、团队同步和数据安全
- 保留角色化工作台、当前访问者和今日行动清单，作为打开系统后的第一层决策入口

## v0.3.7

### 游客只读体验收口
- 版本号升级到 v0.3.7，并同步 CLI、健康检查、前端版本信息和 README
- 局域网游客的需求列表操作收拢为查看与复制，不再展示编辑、评审协作和任务同步入口
- 需求详情展开面板在游客模式下隐藏编辑与批注评审入口，避免只读用户进入维护流程
- 工作台新增“当前访问”面板，本机用户可以看到本机访问和局域网游客的最近活跃状态
- npm 发布清单收紧为明确文件，避免隐藏临时文件混入发布包

### 权限回归测试
- 新增服务端回归测试，覆盖本机访问与局域网访问的身份识别
- 新增写入门禁测试，确保 `/api/v1/state` 本机可写、局域网游客返回 403

## v0.3.6

### 局域网游客门禁正式化
- 局域网访问按请求来源固定识别为“局域网游客 / 游客”，不再沿用本机保存的身份
- 本机专属入口统一加 `local-only` 标记，游客隐藏工作台、系统设置、AI、同步、导入恢复、新建和批量操作
- 关键前端函数补充本机守卫，防止通过旧界面状态或控制台绕过 UI 限制
- 服务端写入、导入、快照恢复、软件更新、主动同步、任务同步和 AI 执行接口统一限制为本机访问
- 修复本机维护接口的 Fastify preHandler 成功路径挂起问题，确保本机保存和 AI/同步维护接口能正常继续执行

## v0.3.5

### 主题与发布质量
- 版本号升级到 v0.3.5，并同步 CLI、健康检查、前端版本信息和 README
- Light Aurora 主题补齐空状态、同步预览和 AI 提案卡片样式，减少不同页面之间的视觉跳变
- README 增加发布前检查路径，覆盖本机身份、局域网游客、筛选、批量确认、发布计划、AI 提案、同步和快照恢复

## v0.3.4

### AI、同步与数据安全收口
- AI 提案面板补充空状态、提案类型标签和“不直接改库”的确认说明
- 团队同步预览改为直接展示新增、更新、冲突数量，并在存在冲突时提示优先核对
- 快照恢复入口展示当前数据摘要，恢复确认中同时展示当前库和目标快照信息
- 局域网访问者按请求来源自动识别为“局域网游客 / 游客”，不会沿用本机保存的角色身份

## v0.3.3

### 工作台与需求列表
- 工作台新增“今日行动清单”，优先展示逾期、待评审、发布风险和 P0 未完成事项
- 需求列表新增筛选摘要，显示当前筛选条件、结果数量，并支持一键清空筛选

### 批量操作
- 批量变更状态、优先级、加入发布计划前增加确认预览
- 批量确认会展示目标动作、影响数量和前 5 条需求，执行前仍自动创建快照
- 批量删除确认补充被删除需求名称预览，降低误删风险

### 发布计划
- 发布计划详情升级为“发布包”表达，集中展示负责人、里程碑、目标和发布产物
- 风险清单与签核状态独立成区，发布范围、待加入需求和发布文档保持原有操作路径

## v0.3.2

### 产品可用性收口
- 版本号升级到 v0.3.2，并同步 CLI、健康检查、前端版本信息和 README
- 继续统一产品语言：HubPooL 主路径使用“发布计划 / 发布范围 / AI 提案 / 团队同步 / 数据安全”
- 保留导入导出和需求版本历史里的历史兼容字段，避免破坏现有数据和 CSV 模板

### 批量操作与发布计划
- 批量操作菜单收拢为“常用 / 输出 / 危险操作”三段，并限制最大高度，长菜单可滚动
- “删除所选需求”独立为危险操作，避免和常规批量变更混在一起
- 发布计划详情页补充“发布概览 / 本次发布范围 / 待加入需求 / 发布文档”结构提示

### 安全确认与设置说明
- 关键确认弹窗统一展示操作者、影响范围和追溯记录
- CSV 批量导入、项目删除、发布计划删除和团队同步补充更明确的影响范围说明
- 系统设置补充游客工作台可见性、角色作用边界和数据安全快照说明

## v0.3.0

### 发布就绪工作台
- 新增工作台首页，集中展示需求规模、待评审、逾期、发布风险、P0 未完成和最近操作
- 工作台作为默认入口，保留需求、原型、版本和看板视图

### 本地身份与操作追溯
- 系统设置新增本地身份，作为批注、版本历史和操作日志的默认署名
- 新增服务端操作日志，覆盖导入、导出、快照、同步、软件更新和 AI 提案状态等关键动作
- 导入导出 JSON 备份会携带操作日志，便于团队迁移和审计

### 发布计划
- 版本对象新增发布目标、发布负责人、风险等级、风险清单、签核状态和发布说明字段
- 版本卡片和版本详情展示发布目标、风险和签核信息
- P2P 同步继续携带版本扩展字段，避免发布计划在多实例间丢失

### 安全确认与 AI 提案
- 关键操作确认文案统一展示操作者、影响范围和日志记录提示
- AI 提案继续保持“待确认后人工应用”的安全边界

## v0.2.0

### 云同步（新增，真实后端实现）
- 服务实例之间通过 HTTP 直接推送/拉取/合并数据（无第三方云厂商依赖）
- 冲突解决采用"最后更新时间优先"策略，按需求与版本逐条合并
- 每次同步尝试（成功或失败）都记录到 `sync_history`，可在界面查看历史
- 新增连接测试、同步状态摘要接口
- 同步/拉取按钮和弹窗按线条图标、现代极简风格重新设计，替换此前仅做样式展示、未接后端的占位实现

### 系统设置
- 版本号来源改为读取真实运行版本（此前硬编码显示 1.0.0）
- 拓展为 6 个分类标签：网络、云同步、软件更新、项目、数据与存储、协同与版本
- 新增"当前运行状态"卡片、云同步摘要卡片、数据统计卡片
- 网络与项目分类支持"重置本页为默认值"

### 左侧菜单
- 新增项目搜索框、排序（名称/优先级/需求数/逾期数）
- 已归档项目此前虽可在项目设置中勾选，但从未在任何地方生效；现已实际接入筛选，默认隐藏，可一键显示/隐藏并显示归档项目数量
- 新增可折叠侧边栏，折叠状态会记忆（localStorage）
- 项目条目新增头像缩写、优先级色点、逾期需求数角标

### 需求详情展开面板
- 由纯文本网格重新设计为卡片式布局，字段分组更清晰
- 操作按钮（编辑 / 批注评审 / 复制至 Jira / 收起）改为线条图标 + 文案

### 批量选择工具条
- 重新设计为分组布局（已选计数 / 状态变更组 / 版本加入组 / 删除 / 清除选择）
- 删除、清除操作改为图标按钮，视觉上更克制

### 项目设置（编辑弹窗）
- 新增只读"项目概览"：需求数、逾期数、版本数，辅助归档/删除决策
- 项目代码输入实时过滤为大写字母与数字，保存前二次校验格式
- 环境系统与自定义链接的 URL 自动补全协议前缀（如 `example.com` → `https://example.com`）
- 项目下存在需求时，删除按钮直接置灰并提示原因，而非仅在点击后才提示
- 新增"查看该项目需求"快捷按钮，直接跳转到该项目的需求列表

## v0.1.x

- 从单文件 HTML 工具迁移为本地 Node.js 服务 + SQLite 持久化架构
- 多项目需求管理、列表 / 看板 / 版本视图、批量操作
- 评审模式、周报、CSV/JSON 导入导出
- Jira 五段式复制、AI 需求评审（复制往返，不保存 API Key）
- 活态批注讨论（产品/设计/研发/测试/业务）
- 需求级版本历史（操作者、变更原因、字段差异）
- 全库 SQLite 快照与恢复

详见 [ROADMAP.md](./ROADMAP.md) 了解后续规划与明确不在近期版承诺的能力。
